# Python 3.11+ required!

# Control Bot
aiogram>=3.3.0

# Worker Accounts
telethon>=1.34.0

# Tdata: tgconvertor[tdata] → opentele → PyQt5; при «TData support requires opentele» или ImportError
# проверьте: python -c "from opentele.td import TDesktop"
# Ремонт: pip install --force-reinstall "PyQt5>=5.15" "PyQt5-sip>=12.15" telethon opentele
tgconvertor[tdata]>=0.1.4
PyQt5>=5.15.2
PyQt5-sip>=12.13.0

# Database
sqlalchemy>=2.0.48
aiosqlite>=0.19.0
# Нативное расширение для SQLAlchemy (async); без него — «No module named greenlet._greenlet»
greenlet>=3.0.0

# Utilities
aiohttp>=3.9.0
aiofiles>=23.2.0
python-dotenv>=1.0.0
loguru>=0.7.0
fastapi>=0.115.0
uvicorn>=0.30.0
PyJWT>=2.9.0
passlib[bcrypt]>=1.7.4
pytest>=8.0.0
cryptography>=42.0.0

# Proxy support for Control Bot
aiohttp-socks>=0.10.0
