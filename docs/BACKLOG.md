# Бэклог и замечания по коду

Актуально на апрель 2026. Сжатый список того, что стоит доработать (часть пунктов перенесена из старых `ROADMAP.md` / `TASKS_FIX.md`).

## Исправлено в ходе последних правок

- **Конфликт `cancel`:** раздельные callback — `cancel_accounts`, `cancel_mailing`, `cancel_proxy`; для старых сообщений — fallback `cancel` → главное меню (`bot/main.py`).
- **Двойной `callback.answer()`** в `account_view` при «аккаунт не найден» — убран лишний ответ.
- **Удаление аккаунта:** при отсутствии записи в БД больше не показывается сообщение «удалён»; путь к сессии через `SESSIONS_DIR` из конфига.
- **Коллизия callback `account_delete_*` и `account_delete_confirm_*`:** финальное удаление по шаблону `^account_delete_\d+$` (только числовой id).
- **`proxy_assign`:** ответ на callback после успешной смены прокси (раньше на успехе ответ мог не отправляться).
- **Рефакторинг:** `bot/handlers/accounts/` — пакет модулей вместо одного огромного файла.
- **Дублирование DB-сессии:** единый контекст `database.session.session_scope()` (обёртка над `db.async_session_maker()` + rollback при исключении); хендлеры и `workers/manager.py` переведены с пары `db.engine.begin()` + `sessionmaker(conn)`.
- **Рассылка / один аккаунт:** в `WorkerManager.start_mailing()` добавлена ротация аккаунтов по `messages_per_batch`; больше нет стопа кампании из-за исторического «лимита на аккаунт».
- **`typing_enabled` в рассылке:** используется флаг `mailing.use_typing` (не захардкожено в `start_mailing()`).
- **Пагинация:** реализована в длинных списках аккаунтов и прокси.
- **Пагинация клиентов и рассылок:** добавлены `clients_list_p_*` и `mailing_list_p_*` с навигацией страниц.
- **Graceful shutdown:** в `main.py` добавлены остановка активной рассылки и `disconnect_all()` для воркеров перед финальным выходом.
- **Тестовая база:** добавлены `pytest.ini` и smoke-тесты (`tests/test_backlog_fixes.py`) на ключевые функции навигации и шаблонов.

## Высокий приоритет

1. **Модуль «База данных»** (замена «Клиенты»): спецификация и ответы на уточнения — [DATABASE_MODULE_SPEC.md](DATABASE_MODULE_SPEC.md). Далее — поэтапная реализация (схема БД, классы-счётчики, листы, рассылка по классам, поиск, бэкапы, логи приложения).
2. **Тесты (pytest):** расширить от smoke-тестов к регрессионным сценариям (рассылка, импорт, группы, прокси, нейрочат).
3. **Пагинация:** точечно добавить для остальных длинных экранов (по мере роста данных).

## Средний приоритет

- Уточнить единые лимиты/тайминги и добавить smoke-check сценарии перед запуском рассылки.
- Дублирование логики API credentials (если есть в нескольких местах) — централизовать.

## Низкий приоритет / идеи

- Кэш или фоновое обновление `photo_count` на карточке после операций с аватарками.
- Единый стиль `parse_mode=ParseMode.HTML` во всех ответах бота.

---

Исторические детальные роадмапы удалены из корня репозитория в пользу этого файла и [ARCHITECTURE.md](ARCHITECTURE.md).
