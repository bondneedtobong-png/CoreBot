# CoreBot dev/test tooling (task 10). Runtime deps stay in requirements.txt.
# Install: pip install -r requirements.txt -r requirements-dev.txt
# (CI installs both; local full gate needs both too.)
#
# pytest is ALSO in requirements.txt (used for on-box runs); pinned here
# for the CI/dev gate. Keep the two entries compatible when bumping.

pytest>=8,<10
ruff>=0.8,<0.14
# Fixes 2 pre-existing tests/test_tdata_web_import.py failures:
# FastAPI UploadFile/form parsing requires python-multipart at import time.
python-multipart>=0.0.18
# Explicit pins for TestClient transport and fleet YAML/template tests
# (fastapi pulls httpx transitively; tests import yaml/jinja2 directly,
# neither is in requirements.txt).
httpx>=0.27
pyyaml>=6
jinja2>=3.1
